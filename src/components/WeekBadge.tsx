// WeekBadge is deprecated — all weeks are now "regular".
// Kept as a no-op for backward compatibility with any remaining references.
export default function WeekBadge({ type: _type }: { type: string }) {
  return null;
}
