// WeekBadge is deprecated — all weeks are now "regular".
// Kept as a no-op for backward compatibility with remaining references.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function WeekBadge({ type }: { type: string }) {
  return null;
}
